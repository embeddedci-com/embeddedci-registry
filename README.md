# embeddedci-registry

The **embeddedci-registry** contains board definitions, build packs, and scripts used by the **EmbeddedCI platform**.

This repository acts as the **source of truth for supported hardware and build environments** used by the EmbeddedCI client and agents.

