# Example (build definition pack entry):
#   packs:
#     - id: apps/build
#       config:
#         cmd: make
#         artifact: selftest
type: app
scripts:
  build: scripts/build.sh

default_vars: {}

dependencies:
  - pack: fetch
    dest: "{{artifact}}"
    src: "{{src}}"
    optional: true

artifacts:
  - path_var: "out/app-build/{{artifact}}"
    name: "{{artifact}}"

config:
  cmd: "{{cmd}}"
  src: "{{src}}"
  artifact: "{{artifact}}"
  out: "{{out}}"
  dst: "{{dst}}"
